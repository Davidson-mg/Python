ir no settigs do projeto e adicionar a variavel:
STATIC_ROOT = os.path.join(BASE_DIR, 'static')

criar o arquivo static no pycharm
no pycharm: python manage.py collectstatic


sudo apt update
sudo apt upgrade

# Instalando
sudo apt install python3-pip python3.7 python3.7-dev python3.7-venv gcc default-libmysqlclient-dev libssl-dev nginx curl



# Atualizando pip e demais
python3.7 -m pip install --upgrade pip setuptools wheel --user
export PATH="/home/$USER/.local/bin:$PATH"
python3.7 -m pip install pipenv --user

# Criar pasta
mkdir agenda
# Entrar na pasta
cd agenda



# Criando o ambiente virtual
python3.7 -m venv venv
source venv/bin/activate
python3.7 -m pip install django gunicorn pillow


####se tiver o problema Python No Module Named Pil #####
python3 -m pip install --upgrade pip
python3 -m pip install --upgrade Pillow
######################################################


gunicorn --bind 0.0.0.0:8000 agenda.wsgi

se retornar erro de time_zone, para a região de sp o certo é: 	Brazil/East

deactivate
exit


pip install django pillow django-summernote mysql_client gunicorn (neste caso, vai dar alguns erros, especilamente o summernote, mas é normal)


